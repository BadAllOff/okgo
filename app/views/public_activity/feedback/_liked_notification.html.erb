<li>
  <div class="pull-left user-block col-xs-2">
    <%= show_profile_img(activity.owner, 'small', "bg-maroon", "", false) %>
  </div>
  <div class="col-xs-10">
    <h5>
      <%= link_to activity.owner.profile.firstname, profile_path(activity.owner) %>
      <small class="pull-right"><i class="fa fa-clock-o"></i> <%= time_ago_in_words(activity.created_at, include_seconds: true) %></small>
    </h5>
    <p>
      <% if activity.trackable %>
          <%= t'notifications.feedback.liked_feedback' %> <%= link_to (t'notifications.feedback.feedback'), feedback_path(activity.trackable) %>
      <% else %>
          <%= t'notifications.feedback.liked_feedback_which_has_since_been_removed' %>
      <% end %>
    </p>
    <p class="clearfix"></p>
  </div>
</li>