<li>
  <div class="pull-left user-block col-xs-2">
    <%= show_profile_img(activity.owner, 'small', "bg-green", "", false) %>
  </div>
  <div class="col-xs-10">
    <%= render partial: 'shared/notifications/header', locals: { activity: activity } %>
    <p>
      <% if activity.trackable %>
          <%= t'notifications.feedback.comment.commented_to' %> <%= link_to 'feedback', feedback_path(activity.trackable) %>
      <% else %>
          <%= t'notifications.feedback.comment.commented_to_feedback_which_has_since_been_removed' %>
      <% end %>
    </p>
    <p class="clearfix"></p>
  </div>
</li>